\documentclass[margin=5mm]{standalone}
\usepackage[dvipsnames]{xcolor}
\usepackage{pgfplots}
\usepackage{tikz}

\pgfplotsset{compat=1.18}
\begin{document}

\begin{tikzpicture}
    \begin{axis}[
        align = center,
        xlabel = Overall time in seconds,
        ylabel = XNAT version,
        xtick pos = left,
        ytick pos = left,
        ytick = data,
        xmin = 0,
        enlarge x limits = {value = 0.2, upper}, % make node labels not overlap with axes
        title = {CSTORE of 2000 MR images},
        title style = {text width = 0.8\textwidth},
        nodes near coords,
        nodes near coords align = horizontal,
        symbolic y coords = {1.8.9.2-att2},
        xbar,
        area legend,
        legend pos = outer north east,
        legend entries = {standard,with site anon},
        bar width = 0.3cm,
        y = 1.0cm,
        point meta=x
    ]
        \addplot[xbar, fill = blue!20!white] coordinates { (91.3,1.8.9.2-att2) };
        \addplot[xbar, fill = black] coordinates { (146.9,1.8.9.2-att2) };
    \end{axis}
\end{tikzpicture}

\end{document}